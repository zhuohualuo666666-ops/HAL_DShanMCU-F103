<!-- 2026.1.5  STM32基础知识——中断 -->

中断的流程：
1.保存现场
2.处理异常事件
3.恢复现场

中断的作用：
及时处理突发事件，防止紧急事件未及时处理导致数据/信号等丢失

例：
采用轮询方式，读取gpio的引脚值判断按键是否按下，
如果在按下按键之前读取按键值为1，表示按键未按下，
而按下按键时，程序执行到其他功能未读取按键值，
下次读取时，按键已复位，读取的值仍为1，
那么就丢失了中间这次按下按键的操作。

引入中断可以及时处理按键操作不丢失
